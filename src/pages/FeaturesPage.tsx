import InfoPage from './InfoPage'

export default function FeaturesPage() {
  return (
    <InfoPage
      label="FEATURES"
      title="Noise-free networking and recruiting"
      paragraphs={[
        'Denoisr is designed to remove the informational clutter that makes professional networking ineffective.',
        'Instead of feeds and engagement loops, Denoisr uses intent-driven matching and relevance-first discovery.',
      ]}
    />
  )
}

